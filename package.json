{
  "name": "ts-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist && rm -rf .cache",
    "start": "yarn clean && npx nodemon ./index.html",
    "build": "yarn clean && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "parcel": "^2.3.2",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.6.2"
  }
}
